# Modelos

Coloque aqui os modelos 3D do teste:

- `casa.glb` — mapa da casa.
- `handvr.glb` — mão virtual que será ligada ao tracking.

Não altere os nomes dos arquivos para manter o carregamento automático nas próximas etapas.
